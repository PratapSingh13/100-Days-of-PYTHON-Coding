#Creating a String
pancard_number="AABGT6715H"

#Getting the Length of the String
print(Length of the PAN Card Number is:",len(pancard_number))

#Concatenation of two Strings
      name1="PAN"
      name2="CARD"
      name=name1+name2
      print(name)

#Iterating the String using range()
      print("Iterating the String using range()")
      for index in range(0,len(pancard_number)):
      print(pancaard_number[index])
            
#Iterating the String using in keyword
      print("Iterating the String using keyword in")
      for value in pancard_number:
      print(value)
#Searching for Character in String
      print("Searching for the Character in String")
      if "Z" in pancard_number:
          print("Character Is Present")
      else:
          print("Character is not Present")

#Slicing a String
      print("The Number in PAN Card is:",pancard_number[5:9])
      print("Last but one 3 Characters in PAN Card:",pancard_number[-4:-1]
            
      
      
  
